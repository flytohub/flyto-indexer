# Tests

This directory contains unit, regression, architecture, and integration tests
for Flyto2 Indexer.

Tests pin scanner accuracy, no-runtime-dependency packaging, MCP protocol
safety, release gates, rules policy, and self-verification behavior.
