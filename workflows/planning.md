# Planning Workflow

1. Read `STATE.md`, `ARCHITECTURE.md`, and `DECISIONS.md`.
2. Map the change through scan, index storage, query API, CLI, MCP, and tests.
3. Decide whether the result should be warning, failure, or report-only.
4. Include generated-artifact and no-network implications.
