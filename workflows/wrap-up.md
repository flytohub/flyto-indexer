# Wrap-Up Workflow

1. Run `bash scripts/lint-project-memory.sh`.
2. Run relevant lint, type, test, verify, or build gates.
3. Update `STATE.md`, `tasks.md`, and `DECISIONS.md` when durable behavior
   changes.
4. Register handoff context in `handoffs/_registry.md`.
