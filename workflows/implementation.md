# Implementation Workflow

1. Inspect current analyzer and verify patterns before editing.
2. Add tests for positive, negative, and empty-repo cases.
3. Keep output structured enough for agents and CI.
4. Run project memory lint plus relevant lint, type, test, and verify gates.
