# Idea Capture Workflow

1. Name the repository insight or verification gap.
2. Identify whether it belongs in scanner, index, query, audit, verify, or MCP.
3. Define the smallest local-only signal that would make the result actionable.
