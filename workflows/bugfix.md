# Bugfix Workflow

1. Capture the input repo shape that caused the wrong result.
2. Determine whether the defect is scan, parse, index, query, or presentation.
3. Add a regression fixture that avoids sensitive customer content.
4. Update state if the bug affects Flyto2 release verification.
