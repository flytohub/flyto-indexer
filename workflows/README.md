# Workflows

These workflow notes define how indexer work should move from question to
verified result.

- Use `investigation.md` for uncertain analysis or false positives.
- Use `planning.md` before changing index schemas, verify behavior, or MCP APIs.
- Use `implementation.md` for scoped code changes.
- Use `wrap-up.md` to record verification and durable state.
