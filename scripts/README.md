# Scripts

This directory contains CI and release helper scripts.

Scripts should be deterministic, local-first where possible, and safe to run
from automation. Prefer explicit arguments over environment assumptions.
