# Configuration

This directory contains local scanner, rule, and project configuration used by
the Flyto2 Indexer CLI, MCP server, and CI verification gates.

Configuration files are source-controlled policy inputs. Generated indexes,
workspace caches, and customer scan data must stay outside this directory.
