# Examples

This directory contains small runnable examples for local audits, incremental
indexing, and quality checks.

Examples may show usage patterns, but production scanner behavior belongs in
`src/` with tests in `tests/`.
